#include "main.h"
/**
 *_abs - entry point
 *
 * @n: character to be checked
 * Return: 1 if positive, -1 if negative, 0 if otherwise
 */
int _abs(int n)
{
if (n > 0)
{
return (1);
}
else if (n < 0)
{
return (-1);
}
else
{
return (0);
}
}
