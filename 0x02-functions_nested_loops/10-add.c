#include <stdio.h>
#include "main.h"
/**
 * add - Entry point
 * @a: int to add
 * @b: int to add
 * Description@ A function that adds two integers and returns the result
 * Return: sum of two integers
 */
int add(int a, int b)
{
return (a + b);
}
