#include <stdio.h>
/**
 * main - Entry point of a program that prints a line with the print function
 *
 * Return: 0 (successful)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
