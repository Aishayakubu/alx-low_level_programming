#include <stdio.h>
#include "main.h"
/**
 * mul - Entry point
 * @a: character to be checked
 * @b: character to be checked
 * Description@ multiplication
 * Return: multiplicaton of two numbers
 */
int mul(int a, int b)
{
return (a * b);
}
