#include <stdio.h>
#include <string.h>
/**
 * _strlen - entry point
 * @s: character to be checked
 * Return: 0
 */
int _strlen(char *s)
{
int len;
s = "my first string";
len = strlen(s);
printf("%d\n", len);

return (0);
}
